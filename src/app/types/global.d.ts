export type User = {
  username: string;
  email: string;
  id: number;
  firstName: string;
};

export type UserNotification = {
  name: string;
  data: string;
  id: number;
  createdDate: string;
};